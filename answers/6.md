### Ответ 1

```js

function task(callback) {
  
  setTimeout(() => {
    callback()
    console.log('Task completed');
  }, 2000);
}

function onCompleted() {
  console.log('All tasks completed')
}

task(onCompleted);
```
